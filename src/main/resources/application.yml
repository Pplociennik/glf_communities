server:
  port: 8090

spring:
  application:
    name: glf-communities
  profiles:
    active: dev

  config:
    import:
      - "configserver:http://configserver:8071/"
      - "classpath:buildInfo.yml"
      - "classpath:buildEnvInfo.yml"

springdoc:
  api-docs:
    path: /api-docs

info:
  app:
    name: "glf-communities"
    description: "Goaleaf communities application."
    version: "0.0.1-SNAPSHOT"